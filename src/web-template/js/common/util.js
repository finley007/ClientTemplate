var app = app || {};
/** @namespace */
app.Util = app.Util || {};
(function($) {
    "use strict";
    app.Util = {
        /**
         * version name
         * @type {String}
         */
        version: "v2",
        /**
         * locale active default locale
         * @type {String}
         */
        locale: "us-en",
        /**
         * Trigger Initial methods when js get loaded.
         */
        init: function() {

        }

    };
    app.Util.init();

})(jQuery);