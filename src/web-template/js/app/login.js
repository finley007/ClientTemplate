"use strict";
// 登录的相关逻辑在该模块中实现
define(function(require) {

	var $ = require('jquery');

    var loginModule = {
    	//初始化界面
        initPage: function() {
            console.log("Init login page");
            // $("body").append("aa");
        }

        //监听事件
        
    };

    return loginModule;
});